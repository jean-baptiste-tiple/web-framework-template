# Spec page — <Nom de la page>

- Type : home / page / landing / article
- URL : /<slug>
- Objectif & CTA :
- Intention SEO (requête cible) :
- Sections (si landing) : hero -> features -> proof -> faq -> cta
- JSON-LD attendu : Organization+WebSite (auto) + <BlogPosting/WebPage/Service> + FAQPage? + Breadcrumb
- tldr :
- faq : [q/a]
- Assets : images (alt, dimensions)
- Rayon d'impact (obligatoire si la page crée une surface : composant, variante, collection, champ Zod, route) : dérouler .claude/templates/rayon-impact.md (appelants / doublons / effet produit / refacto)
